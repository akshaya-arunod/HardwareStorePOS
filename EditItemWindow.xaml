<Window x:Class="HardwareStore.Views.EditItemWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit Item"
        Width="460"
        Height="614"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F9FAFB"
        FontFamily="Segoe UI"
        FontSize="14">

    <Border Background="White"
            CornerRadius="10"
            Margin="16"
            Padding="20"
            BorderBrush="#E5E7EB"
            BorderThickness="1">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <!-- Header -->
                <RowDefinition Height="*" />
                <!-- Form -->
                <RowDefinition Height="Auto" />
                <!-- Buttons -->
            </Grid.RowDefinitions>

            <!-- Title -->
            <TextBlock Grid.Row="0"
                       Text="Edit Item"
                       FontSize="20"
                       FontWeight="SemiBold"
                       Foreground="#111827"
                       Margin="0 0 0 16"/>

            <!-- Form -->
            <StackPanel Grid.Row="1" VerticalAlignment="Top">
                <!-- Item Name -->
                <TextBlock Text="Item Name" Margin="0 6 0 2"/>
                <TextBox x:Name="ItemNameTextBox"
                         Height="34"
                         Margin="0 0 0 10"
                         Padding="8"
                         BorderBrush="#D1D5DB"
                         VerticalContentAlignment="Center"/>

                <!-- Category -->
                <TextBlock Text="Category" Margin="0 6 0 2"/>
                <ComboBox x:Name="CategoryComboBox"
                          Height="34"
                          Margin="0 0 0 10"
                          Padding="4"
                          BorderBrush="#D1D5DB"
                          VerticalContentAlignment="Center"
                          DisplayMemberPath="Name"/>

                <!-- Price -->
                <TextBlock Text="Price (LKR)" Margin="0 6 0 2"/>
                <TextBox x:Name="PriceTextBox"
                         Height="34"
                         Margin="0 0 0 10"
                         Padding="8"
                         BorderBrush="#D1D5DB"
                         VerticalContentAlignment="Center"/>
                <!-- Discount (%) -->
                <TextBlock Text="Discount (%)" Margin="0 6 0 2"/>
                <TextBox x:Name="DiscountTextBox"
                 Height="34"
                 Margin="0 0 0 10"
                 Padding="8"
                 BorderBrush="#D1D5DB"
                 VerticalContentAlignment="Center"/>

                <!-- Stock -->
                <TextBlock Text="Stock" Margin="0 6 0 2"/>
                <TextBox x:Name="StockTextBox"
                         Height="34"
                         Margin="0 0 0 10"
                         Padding="8"
                         BorderBrush="#D1D5DB"
                         VerticalContentAlignment="Center"/>

                <!-- SKU (Read-Only) -->
                <TextBlock Text="SKU (Not Editable)" Margin="0 6 0 2"/>
                <TextBox x:Name="SKUTextBox"
                 Height="34"
                 Margin="0 0 0 10"
                 Padding="8"
                 Background="White"
                 BorderBrush="#D1D5DB"
                 Foreground="#6B7280"
                 VerticalContentAlignment="Center"
                 IsReadOnly="True"
                 IsEnabled="False"
                 BorderThickness="1"/>      
            </StackPanel>

            <!-- Buttons -->
            <StackPanel Grid.Row="2"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="0 20 0 0">
                <Button Content="Cancel"
                        Width="80"
                        Height="34"
                        Margin="0 0 10 0"
                        Background="#E5E7EB"
                        Foreground="#374151"
                        BorderBrush="#D1D5DB"
                        Click="CancelButton_Click"/>

                <Button Content="Save"
                        Width="80"
                        Height="34"
                        Background="#3B82F6"
                        Foreground="White"
                        BorderBrush="#2563EB"
                        Click="SaveButton_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
